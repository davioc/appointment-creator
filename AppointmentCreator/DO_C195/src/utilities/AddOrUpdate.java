package utilities;

/**
 * This <code>ENUM</code> is used for switch statements to load add or update forms. It's used for the Appointment and Customer form to determine if the methods
 * used are for adding or updating an object.
 */
public enum AddOrUpdate {

    ADD,
    UPDATE

}
